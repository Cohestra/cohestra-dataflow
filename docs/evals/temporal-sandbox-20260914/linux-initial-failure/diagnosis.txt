The community restart query exceeded its10-second HTTP deadline while the stopped worker retained a workflow task until its10-second start-to-close expiry. History events52–53 show the old worker task and exact timeout; event54 is test cleanup termination. All other community scenarios and all enterprise scenarios passed. The test now treats only GET context deadline expiration as a retryable poll result under the existing bounded scenario deadline, still requiring real paused control state. No mutation retries or production behavior changes.
